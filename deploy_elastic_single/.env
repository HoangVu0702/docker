ELASTIC_PASSWORD='Test@2020###'

# Password for the 'kibana_system' user (at least 6 characters)
KIBANA_PASSWORD='RMqYdTzGBIfNr9tDsE29'

# Set the cluster name
CLUSTER_NAME=mini_soc-cluster

# Set to 'basic' or 'trial' to automatically start the 30-day trial
LICENSE=basic
#LICENSE=trial

# Port to expose Elasticsearch HTTP API to the host
ES_PORT=9200
#ES_PORT=127.0.0.1:9200

# Port to expose Kibana to the host
KIBANA_PORT=5601
#KIBANA_PORT=80

# Increase or decrease based on the available host memory (in bytes)
MEM_LIMIT=1073741824

DATA_ES=$(pwd)/es_data
CERTS=$(pwd)/certs
DATA_DASH=$(pwd)/kib_data
CONFIG_LOGSTASH=$(pwd)/logstash_config
CONFIG_DASH=$(pwd)/kib_config

DOMAIN_COLLECT_LOGS=siem.durocket.dev
IP_COLLECT_LOGS1='10.0.0.4'
IP_COLLECT_LOGS2='104.208.112.56'
